服务端目前不处量粘包问题

软件在登录时，应该进行如下操作：
1 发送AT+USERS=用户名，密码\r\n
用户名不能超过20个字符，密码不能超过18个字符


2 登录完成后，主动上传设备ID，发送指令如下
AT+ID=16位的设备ID\r\n

ID验证正确，服务器即返回OK，ID验证不正确即返回ERROR


手动上传ID也可以发送该命令，软件端也是发送该命令


3 透传的命令，<设备ID>，要透传的命令



